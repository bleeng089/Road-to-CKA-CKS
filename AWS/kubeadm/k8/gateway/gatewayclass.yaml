# Cluster-scoped. Says "Gateways of class 'eg' are implemented by Envoy
# Gateway" — the same role a cloud team or platform team would own.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
